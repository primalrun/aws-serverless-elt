# AWS credentials — either set these or configure ~/.aws/credentials
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
AWS_DEFAULT_REGION=us-east-1

# Redshift admin password — also set in terraform/terraform.tfvars
TF_VAR_redshift_admin_password=YourPassword1!
